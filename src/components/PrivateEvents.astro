<section id="private-events">
    <div class="container d-flex">
        <div>
            <h3>Private Events</h3>
            <h4 class="margin-bottom">Let's Get Into It!</h4>
        </div>
    </div>
    <div class="container d-grid private-events-grid">
        <div>
            <ol class="list-split">
                <li>
                    <strong>Volume Driven</strong> (We have accommodated parties of 500 people.)
                </li>
                <li>
                    <strong>Formal or Casual</strong> (We have all kinds of clothes. Don't sweat it.)
                </li>
                <li>
                    Excellent for parties, the beach, work parties, golf parties, graduation parties, party parties and party party parties for dogs and animals all around.
                </li>
                <li>
                    Each pie takes 70 seconds to cook (we've timed it) 
                </li>
                <li>
                    We offer a full menu and make pies fresh from scratch, every time.
                </li>
                <li>
                    We match the flow of the guests. Awkward lunch hour? No big, we'll make sure we keep it all fresh for when the swarm arrives.
                </li>
            </ol>
        </div>
        <div>
            <img class="moto-gerald" src="/images/moto-gerald.webp" alt="VROOOOOM!">
        </div>
    </div>
</section>

<style>

section#private-events {
        padding-block: 3rem;
    }

#private-events {
    background-color: var(--tg-light-background);
}

.private-events-grid {
    grid-template-columns: 2.7fr 1fr;
    grid-template-rows: 1fr;
}

.list-split {
    columns: 2;
    gap: 3.5rem;
    font-size: 1.7rem;
}

@media (max-width: 1000px) {
    
    .private-events-grid {
        grid-template-columns: 1fr;
        grid-template-rows: 1fr;
    }

    .list-split {
        columns: 1;
        gap: 0;
        font-size: 1.5rem;
    }

}

#private-events h3 {
    font-family: var(--font-headline);
}

.moto-gerald {
    animation: ride infinite ease-in-out 400ms;
}

.margin-bottom {
  margin-bottom: -1rem;
}

@keyframes ride {
    0% {
        transform: translateX(-10px);
        transform: translateY(0px);
    }
    50% {
        transform: translateY(15px);
    }
    100% {
        transform: translateY(0px);
    }
}

</style>